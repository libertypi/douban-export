# Export All Your Douban Movie Ratings

导出某个人豆瓣电影上的所有评分，存为 JSON 和 Markdown 格式。

### Usage:
```bash
python douban.py <douban_username>
```

The results will be saved as JSON and Markdown files in `<script_directory>/output/`.

Run `python douban.py -h` for more options.

### Author
David P.
